#lang planet neil/sicp

;; in this function/procedure :
;; 1- if predicate statement >> return (+ or -) procedure
;; 2- this built-in procedure will be applied to (a) and (b) as its parameters

;; ** Higher-order function we can return it/take it as a parameter/assign it to variable